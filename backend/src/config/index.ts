export const config = {
  canCross: true,
  allowRandomHalfSentence: true,
};

export default config;

export const canCross = config.canCross;
export const allowRandomHalfSentence = config.allowRandomHalfSentence;
